name: codync-host

on:
  push:
    branches: [main, v2]
    paths: ["host/**", "linux/**", ".github/workflows/host.yml"]
    tags: ["host-v*"]
  pull_request:
    paths: ["host/**", "linux/**"]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: host
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: host
      - run: cargo clippy -- -D warnings
      - run: cargo test

  linux-app:
    strategy:
      matrix:
        include:
          - { os: ubuntu-24.04, target: x86_64-unknown-linux-gnu }
          - { os: ubuntu-24.04-arm, target: aarch64-unknown-linux-gnu }
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    defaults:
      run:
        working-directory: linux
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y libgtk-4-dev libadwaita-1-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: linux
      - run: cargo clippy -- -D warnings
      - run: cargo test
      - name: Package
        if: startsWith(github.ref, 'refs/tags/host-v')
        run: |
          VERSION=${GITHUB_REF_NAME#host-v}
          NAME=codync-linux-$VERSION-${{ matrix.target }}
          cargo build --release
          mkdir -p $NAME/share/applications $NAME/share/icons/hicolor/scalable/apps $NAME/bin
          cp target/release/codync $NAME/bin/
          cp data/com.pokai.Codync.desktop $NAME/share/applications/
          cp data/com.pokai.Codync.svg $NAME/share/icons/hicolor/scalable/apps/
          tar czf ../$NAME.tar.gz $NAME
      - uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/host-v')
        with:
          files: codync-linux-*.tar.gz

  release:
    if: startsWith(github.ref, 'refs/tags/host-v')
    needs: test
    strategy:
      matrix:
        include:
          - { os: ubuntu-latest, target: x86_64-unknown-linux-gnu }
          - { os: ubuntu-24.04-arm, target: aarch64-unknown-linux-gnu }
          - { os: macos-latest, target: aarch64-apple-darwin }
          - { os: macos-latest, target: x86_64-apple-darwin }
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Build
        working-directory: host
        run: cargo build --release --target ${{ matrix.target }}
      - name: Package
        run: |
          VERSION=${GITHUB_REF_NAME#host-v}
          NAME=codync-host-$VERSION-${{ matrix.target }}
          mkdir $NAME
          cp host/target/${{ matrix.target }}/release/codync-host $NAME/
          cp LICENSE $NAME/
          tar czf $NAME.tar.gz $NAME
          shasum -a 256 $NAME.tar.gz > $NAME.tar.gz.sha256
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            codync-host-*.tar.gz
            codync-host-*.tar.gz.sha256
